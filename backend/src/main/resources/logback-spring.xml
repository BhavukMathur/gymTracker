<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Logback: appenders, layout, and MDC (requestId from filter; traceId/spanId from Micrometer Tracing). -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <property name="CONSOLE_LOG_PATTERN"
              value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] reqId=%X{requestId:-} traceId=%X{traceId:-} spanId=%X{spanId:-} %logger{36} - %msg%n"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
